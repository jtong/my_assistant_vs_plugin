project:
  base_path: ./
  filters:
    - filter_in:
      - ".ai_helper/agent/chat/*"
      # - ".ai_helper/agent/chat/roleplay_app/*"
      # - ".ai_helper/agent/chat/node_modules/**"
      - "node_modules/**"
      - "chat/**"
      - "*.*"
      - ex_command/**
    - ignore:
      - .ai_helper
      - node_modules/@(.*)
      - node_modules/!(ai-agent-response)
      - ".ai_helper/agent/chat/node_modules/!(ai-agent-response|@jtong)"
      # - ".ai_helper/agent/chat/node_modules/!(@jtong/knowledge_query)"
      - config.yml
      - "**/*.png"
      - "**/.DS_Store"
      - ".ai_helper/**/debug-agent.log"
      - ".ai_helper/**/.DS_Store"
      - ".ai_helper/**/LICENSE.txt"
      - "**/LICENSE.txt"
      - ".ai_helper/**/package-lock.json"
      - "**/package-lock.json"
      - "*/test"
      - "*.yml"
      - "**/*.log"
      - "*.log"
      - "chat/webview/lib"
      - "**/*.vsix"
      - doc
      - README.md
read_only_references:
  - $ref: "/Users/jtong/develop/education/homework-lab/java_microservice/prompt-builder-projects/personal_knowledge/read_only_reference/format/source_code.yml#file"      
input:
  git_clone_to_path: .ai_helper/prompt_builder/git_repo
  instruction: |
    ## 任务约束
    
    - 分清webview的代码和插件的代码和agent app的代码
    - 按照ai_gen:file输出，严格遵守标签规范。

    ## 任务
    我希望 把origin和change_to换成
    <<<<<<< SEARCH
    return (
      <div>
    =======
    function handleSubmit() {
      saveData();
      setLoading(false);
    }

    return (
      <div>
    >>>>>>> REPLACE

    insert不需要改

    因为AI生成的时候=号和<号、>号未必能保证是7个，你要用更智能的方式判断，语法上我们可以保证这几个记号肯定独占一行。

    因为这些字符默认独占一行，但代码中可能存在最后一个字符不是换行符的情况，所以，我们要在读取search和replace内容的时候统一截取最后一个换行符
output:     
  prompt:
    path: .ai_helper/prompt_builder/output/working
    backup_path: .ai_helper/prompt_builder/output/backup

# 我希望 在webview的chat 窗口里加入搜索功能，可以跳到对应的文本对应的位置，如果多处匹配，可以上下跳转    