project:
  base_path: ./
  filters:
    - filter_in:
      - app/**
    - ignore:
      - .ai_helper
      - "**/spike"
      - ".git"
      - "*/node_modules"
      - config.yml
      - "**/*.png"
      - "**/.DS_Store"
      - "**/LICENSE.txt"
      - "**/package-lock.json"
      - media
      - "*/test"
      - "*.log"
      - "chat/webview/lib"
      - "*.yml"
      - "**/*.vsix"
      - dist
      - temp
      - doc
      - README.md
input:
  instruction: |
    ## 任务约束
    
    - 分清webview的代码和插件的代码和agent app的代码
    - 打调试型日志的不要删掉。
    - 文本也不要删掉。

    ## 任务

    我希望 不要用interface实现现在的效果。我们暂时不使用interface这个实例，专注于用Blocks来实现同样的布局和效果。
output:     
  prompt:
    path: .ai_helper/prompt_builder/output/working
    backup_path: .ai_helper/prompt_builder/output/backup