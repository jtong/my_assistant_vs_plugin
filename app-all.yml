project:
  base_path: ./
  filters:
    - filter_in:
      - app/**
    - ignore:
      - .ai_helper
      - "**/spike"
      - ".git"
      - "*/node_modules"
      - config.yml
      - "**/*.png"
      - "**/.DS_Store"
      - "**/LICENSE.txt"
      - "**/package-lock.json"
      - media
      - "*/test"
      - "*.log"
      - "chat/webview/lib"
      - "*.yml"
      - "**/*.vsix"
      - dist
      - temp
      - doc
      - README.md
input:
  instruction: |
    ## 任务约束
    
    - 分清webview的代码和插件的代码和agent app的代码
    - 打调试型日志的不要删掉。
    - 文本也不要删掉。

    ## 任务

    我希望 block.events 应该不是一个数组，我不知道目前的代码为什么要搞成数组，理论上搞成json更适合。
output:     
  prompt:
    path: .ai_helper/prompt_builder/output/working
    backup_path: .ai_helper/prompt_builder/output/backup